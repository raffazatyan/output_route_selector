name: output_route_selector
description: A Flutter plugin to select audio output routes (speaker, receiver, bluetooth, wired headset) on iOS devices.
version: 1.0.6
homepage: https://github.com/raffazatyan/output_route_selector
repository: https://github.com/raffazatyan/output_route_selector

environment:
  sdk: ^3.10.3
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: OutputRouteSelectorPlugin
